This folder holds validation logic for Domain models.

- Use DataAnnotations on entities for basic constraints.
- Add richer validators (e.g., FluentValidation) here if desired.
- Keep rules aligned with AGENTS.md global guardrails (no cascade deletes, uniqueness, idempotency).

